

import 'dart:ui';

class AppColors {
  // New primary light blue for top gradient (exact new spec).
  static const Color primaryBlue = Color(0xFF00AEEF);

  // Secondary blue not used now, but kept for consistency.
  static const Color secondaryBlue = Color(0xFF0056B3);

  // Button blue - matches primary for consistency.
  static const Color buttonBlue = Color(0xFF00AEEF);

  // New accent blue for labels and button bg (rgba(0,140,192,1)).
  static const Color accentBlue = Color(0xFF008CC0);
}

// Size constants - Unchanged.
class AppSizes {
  static const double standardPadding = 16.0;
  static const double largePadding = 32.0;
}